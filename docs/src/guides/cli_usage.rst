CLI Usage
=========

The CLI is exposed through ``ydana`` and powered by :mod:`ydana.cli`.

Key commands:

- ``ydana fill-histos`` for histogram production.
- ``ydana dump-events`` for ROOT or Parquet export.

Use ``ydana --help`` and ``ydana <command> --help`` for full options.
