Installation
============

Use your preferred Python environment manager, then install YDANA in editable mode.

.. code-block:: bash

   python -m pip install -e .[docs]

You can verify the CLI is available:

.. code-block:: bash

   ydana --help
