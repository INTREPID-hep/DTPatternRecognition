YAML Basics
===========

YDANA is declarative: you configure files, processing, and histogram definitions in YAML.

- ``filesets.yaml`` defines named datasets and file lists.
- ``histograms.yaml`` defines histogram objects and fill expressions.

The next sections explain how these YAML definitions become lazy task graphs.
